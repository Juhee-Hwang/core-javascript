// console.log("hi"); // 여기서는 node를 사용해서 실행한다.

// console.log(window); // 브라우저가 아니라서 에러가 발생한다.

// console.log(global); // 글로벌은 노드 안에 살고 있는 객체라서 정보를 불러온다.

// const liveServer = require("live-server");

// const params = {
//   host: "localhost",
//   port: 3000,
//   open: false,
//   root: "./client",
// };

// liveServer.start(params);
